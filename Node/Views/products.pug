doctype html
html(lang='en')
    head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title #{title}
    //- for setting dynamic value we have to use #{variables we passed in render method second parameter}
    body
        main
            h1 #{title}
        ul
            if allProducts.length > 0
                each product in allProducts
                    li #{product.product}
            else
                h1 No product available
//- we are running a foreach loop with pug syntax